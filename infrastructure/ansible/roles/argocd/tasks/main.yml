- name: Delete all ArgoCD resources (including global)
  shell: |
    kubectl delete -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml --ignore-not-found=true 2>/dev/null || true
    kubectl delete namespace argocd --ignore-not-found=true 2>/dev/null || true
    kubectl delete crd applications.argoproj.io appprojects.argoproj.io --ignore-not-found=true 2>/dev/null || true
    kubectl delete clusterrole argocd-application-controller argocd-applicationset-controller argocd-server argocd-redis argocd-repo-server argocd-dex-server argocd-notifications-controller --ignore-not-found=true 2>/dev/null || true
    kubectl delete clusterrolebinding argocd-application-controller argocd-applicationset-controller argocd-server --ignore-not-found=true 2>/dev/null || true
    sleep 5
  changed_when: false

- name: Force remove existing ArgoCD Helm release and stuck secrets
  shell: |
    helm uninstall argocd -n argocd --ignore-not-found 2>/dev/null || true
    kubectl delete secret -n argocd --field-selector type=helm.sh/release.v1 --ignore-not-found 2>/dev/null || true
    kubectl delete namespace argocd --ignore-not-found 2>/dev/null || true
    sleep 10
  changed_when: false
  failed_when: false

- name: Add ArgoCD Helm repo
  shell: |
    helm repo add argo https://argoproj.github.io/argo-helm
    helm repo update

- name: Install ArgoCD via manifests
  shell: |
    # Удаляем проблемный CRD, чтобы избежать ошибки аннотации
    kubectl delete crd applicationsets.argoproj.io --ignore-not-found 2>/dev/null || true
    # Удаляем аннотацию, если CRD не удалился (на всякий случай)
    kubectl patch crd applicationsets.argoproj.io -p '{"metadata":{"annotations":{"kubectl.kubernetes.io/last-applied-configuration":null}}}' --type=merge 2>/dev/null || true
    kubectl create namespace argocd --dry-run=client -o yaml | kubectl apply -f -
    kubectl apply -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml
  register: argocd_install
  until: argocd_install.rc == 0
  retries: 3
  delay: 10

- name: Wait for all ArgoCD pods
  shell: kubectl wait --for=condition=ready pod --all -n argocd --timeout=300s
  retries: 10
  delay: 10
  register: argocd_wait
  until: argocd_wait.rc == 0

- name: Get ArgoCD admin password
  shell: kubectl -n argocd get secret argocd-initial-admin-secret -o jsonpath="{.data.password}" | base64 -d
  register: argocd_password
  changed_when: false

- name: Show ArgoCD password
  debug:
    msg: "ArgoCD admin password: {{ argocd_password.stdout }}"


- name: Get ArgoCD admin password
  shell: kubectl -n argocd get secret argocd-initial-admin-secret -o jsonpath="{.data.password}" | base64 -d
  register: argocd_password
  changed_when: false

- name: Delete existing StorageClass (if any) to allow recreation with new binding mode
  shell: |
    kubectl delete storageclass yc-network-hdd --ignore-not-found=true
  when: argocd_wait.rc == 0
  changed_when: false

- name: Generate and apply ArgoCD Applications
  template:
    src: app.yaml.j2
    dest: "/tmp/{{ item.name }}.yaml"
  loop:
    - { name: client-app, chart_path: infrastructure/k8s/base/myapp-chart }
    - { name: server-app, chart_path: infrastructure/k8s/base/server-chart }
    - { name: database-app, chart_path: infrastructure/k8s/base/database-chart }

  register: app_files

- name: Apply generated Applications
  shell: kubectl apply -f {{ item.dest }}
  loop: "{{ app_files.results }}"
  when: argocd_wait.rc == 0