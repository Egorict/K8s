server {
    listen 80;
    server_name _;

    root ../;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;  # если файл не найден, отдаём index.html
    }

    location /cs {
        try_files /cs.html =404;
    }
}